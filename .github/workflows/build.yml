name: W2016 TWRP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 检查代码
      uses: actions/checkout@v4

    - name: 释放服务器空间
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"

    - name: 配置编译环境
      run: |
        sudo apt-get update
        sudo apt-get install -y git-core gnupg flex bison gperf build-essential zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 libncurses5-dev x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip bc python2 libtinfo5 libncurses5
        sudo ln -sf /usr/bin/python2 /usr/bin/python

    - name: 安装 Repo 工具
      run: |
        mkdir -p ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        echo "PATH=$HOME/bin:$PATH" >> $GITHUB_ENV

    - name: 同步 TWRP 9.0 源码
      run: |
        PATH=$HOME/bin:$PATH
        mkdir -p ~/twrp && cd ~/twrp
        repo init --depth=1 -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_omni.git -b twrp-9.0
        repo sync -j$(nproc) --force-sync --no-tags --no-clone-bundle

    - name: 克隆并修正设备树
      run: |
        cd ~/twrp
        git clone https://github.com/UPHE16/SM-W2016_twrp_device_tree.git device/samsung/w2016
        cd device/samsung/w2016
        # 修正命名逻辑
        [ -f omni_royceltectc.mk ] && mv omni_royceltectc.mk omni_w2016.mk || true
        grep -rl 'royceltectc' . | xargs sed -i 's/royceltectc/w2016/g' || true
        sed -i 's/omni_royceltectc/omni_w2016/g' AndroidProducts.mk || true

    - name: 注入内核修复参数 & 极致瘦身
      run: |
        cd ~/twrp/device/samsung/w2016
        
        # 1. 清理旧定义
        sed -i '/LZMA_RAMDISK_TARGET/d' BoardConfig.mk
        sed -i '/BOARD_KERNEL_CMDLINE/d' BoardConfig.mk
        sed -i '/BOARD_MKBOOTIMG_ARGS/d' BoardConfig.mk
        sed -i '/BOARD_RECOVERYIMAGE_PARTITION_SIZE/d' BoardConfig.mk
        
        # 2. 注入启动修复参数 (针对黑屏重启问题)
        # 匹配 image_45bd6f.png 中显示的偏移量
        {
          echo "LZMA_RAMDISK_TARGET := true"
          echo "BOARD_KERNEL_CMDLINE := console=ttySAC2,115200n8 androidboot.selinux=permissive androidboot.hardware=samsungexynos7420"
          echo "BOARD_MKBOOTIMG_ARGS := --ramdisk_offset 0x01000000 --tags_offset 0x00000100"
          
          # 极致瘦身参数
          echo "TW_EXCLUDE_ENCRYPTED_BACKUPS := true"
          echo "TW_INCLUDE_CRYPTO := false"
          echo "TW_EXCLUDE_MTP := true"
          echo "TW_NO_LANGUAGES := true"
          echo "TW_EXCLUDE_APEX := true"
          
          # 设定物理分区上限 (约 34MB)
          echo "BOARD_RECOVERYIMAGE_PARTITION_SIZE := 35651584"
        } >> BoardConfig.mk
        
        # 3. 物理删除语言包 (减重核心)
        rm -rf ~/twrp/bootable/recovery/languages/*

    - name: 强制清理并编译
      run: |
        PATH=$HOME/bin:$PATH
        cd ~/twrp
        export ALLOW_MISSING_DEPENDENCIES=true
        source build/envsetup.sh
        lunch omni_w2016-eng
        # 必须 clean 以确保参数生效
        make clean
        make recoveryimage -j$(nproc)

    - name: 上传结果
      uses: actions/upload-artifact@v4
