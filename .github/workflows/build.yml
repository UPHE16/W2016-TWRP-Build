name: W2016 TWRP Slim & Fix Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04 # 使用最快分配的服务器

    steps:
    - name: 检查代码
      uses: actions/checkout@v4

    - name: 腾出磁盘空间
      run: |
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"

    - name: 安装编译环境
      run: |
        sudo apt-get update
        # 安装 Python 2 和 libtinfo5 (解决 image_2bc967.png 中的运行库缺失报错)
        sudo apt-get install -y git-core gnupg flex bison gperf build-essential zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 libncurses5-dev x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip bc python2 libtinfo5 libncurses5
        # 强制将 python 命令指向 python2 (解决 image_026b41.png 中的语法报错)
        sudo ln -sf /usr/bin/python2 /usr/bin/python

    - name: 安装 Repo 工具
      run: |
        mkdir ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        echo "PATH=$HOME/bin:$PATH" >> $GITHUB_ENV

    - name: 初始化并同步源码
      run: |
        PATH=$HOME/bin:$PATH
        mkdir ~/twrp && cd ~/twrp
        # 使用轻量化同步，仅拉取 twrp-9.0 分支
        repo init --depth=1 -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_omni.git -b twrp-9.0
        repo sync -j$(nproc) --force-sync --no-tags --no-clone-bundle

    - name: 克隆并暴力瘦身设备树
      run: |
        cd ~/twrp
        # 使用你自己的设备树仓库
        git clone https://github.com/UPHE16/SM-W2016_twrp_device_tree.git device/samsung/w2016
        cd device/samsung/w2016
        
        # 1. 强制重命名与内部引用修正
        [ -f omni_royceltectc.mk ] && mv omni_royceltectc.mk omni_w2016.mk || true
        grep -rl 'royceltectc' . | xargs sed -i 's/royceltectc/w2016/g' || true
        sed -i 's/omni_royceltectc/omni_w2016/g' AndroidProducts.mk || true

        # 2. 写入强制压缩与瘦身指令 (防止镜像超过 34MB)
        echo "LZMA_RAMDISK_TARGET := true" >> BoardConfig.mk
        echo "TW_EXCLUDE_ENCRYPTED_BACKUPS := true" >> BoardConfig.mk
        echo "TW_EXCLUDE_DEFAULT_USB_INIT := true" >> BoardConfig.mk
        echo "TW_INCLUDE_CRYPTO := false" >> BoardConfig.mk
        echo "TW_NO_REBOOT_BOOTLOADER := true" >> BoardConfig.mk
        
        # 3. 修正打包偏移量 (针对三星 Exynos 7420 优化)
        echo "BOARD_KERNEL_PAGESIZE := 2048" >> BoardConfig.mk
        echo "BOARD_BOOTIMG_HEADER_VERSION := 0" >> BoardConfig.mk
        
        # 4. 删除所有非英文语言包以节省空间 (约节省 3MB)
        rm -rf ~/twrp/bootable/recovery/languages/*

    - name: 开始编译
      run: |
        PATH=$HOME/bin:$PATH
        cd ~/twrp
        export ALLOW_MISSING_DEPENDENCIES=true
        # 显式声明变量，确保编译系统捕获瘦身需求
        export TW_EXCLUDE_ENCRYPTED_BACKUPS=true
        
        source build/envsetup.sh
        lunch omni_w2016-eng
        make recoveryimage -j$(nproc)

    - name: 上传编译产物
      uses: actions/upload-artifact@v4
      with:
        name: W2016-TWRP-Slimmed
        path: ~/twrp/out/target/product/w2016/recovery.img
